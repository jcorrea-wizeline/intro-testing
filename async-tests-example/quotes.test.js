// const {getQuote} = require('./quotes')

describe('getQuote', () => {
  test.todo('resolves the quote version callback')

  test.todo('resolves the quote version returning a promise')

  test.todo('resolves the quote version async await')
})
